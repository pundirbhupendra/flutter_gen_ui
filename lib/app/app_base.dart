import 'package:flutter/material.dart';
import 'package:gen_ui/app/app.dart';

class AppBase extends StatelessWidget {
  const AppBase({super.key});

  @override
  Widget build(BuildContext context) {
    return const App();
  }
}
